name: Docker and Helm

on:
  release:
    types: [published]
  workflow_dispatch:
  
jobs:
  dockerbuild:
    uses: eclipse-xfsc/dev-ops/.github/workflows/dockerbuild.yml@main
    secrets:
      HARBOR_HOST: ${{ secrets.HARBOR_HOST }}
      HARBOR_CREDENTIALS: ${{ secrets.HARBOR_CREDENTIALS }}
    with: 
      dockerfile: "deployment/docker/Dockerfile"
      harbor_config: "deployment/harbor.config"
  dockerbuild-pdf-core:
    uses: eclipse-xfsc/dev-ops/.github/workflows/dockerbuild.yml@main
    secrets:
      HARBOR_HOST: ${{ secrets.HARBOR_HOST }}
      HARBOR_CREDENTIALS: ${{ secrets.HARBOR_CREDENTIALS }}
    with:
      dockerfile: "pdf-core/Dockerfile"
      buildContext: "pdf-core"
      service: "pdf-core"
      harbor_config: "deployment/harbor.config"
  # helm:
  #   uses: eclipse-xfsc/dev-ops/.github/workflows/helm.yml@main
  #   secrets: inherit
  #   with: 
  #     buildContext:  "deployment/helm"
  #     harbor_config: "deployment/harbor.config"