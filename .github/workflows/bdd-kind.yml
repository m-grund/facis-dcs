name: BDD on kind

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  # The BDD (behave) suite and the Playwright E2E each deploy their OWN
  # two-instance kind stack and run in parallel on separate runners: wall-clock
  # is max(bdd, e2e) instead of bdd+e2e, and each gets a full disk/memory budget
  # (the heavy DSS bundle + both DCS instances no longer share one runner).
  bdd-kind:
    permissions:
      contents: read
      checks: write
    runs-on: ubuntu-latest
    # The suite grew from 31 to 50 minutes when the mutual-settlement scenarios
    # gained a full counterparty drive to APPROVED each, and 60 killed it
    # mid-run with the diagnostic dump skipped — a suite that cannot finish
    # reports nothing at all. Raised to leave headroom; the growth itself is
    # worth reducing rather than absorbing indefinitely.
    timeout-minutes: 90
    concurrency:
      group: bdd-kind-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # The Federated Catalogue upgrade scenario installs the last
          # Neo4j-based chart straight out of Git history; the default depth-1
          # clone does not carry that commit's tree.
          fetch-depth: 0

      # The kind node loads the heavy DSS demo bundle (~1-2GB) alongside the two
      # DCS images, pdf-core, and the Postgres/Keycloak/IPFS/ORCE/FC base images;
      # on a stock runner (~14GB free) this intermittently exhausts the disk
      # ("No space left on device" mid pip-install). Reclaim the preinstalled
      # toolchains this job never uses.
      - name: Free up disk space
        run: |
          echo "Before:"; df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
            /opt/hostedtoolcache/CodeQL /usr/local/.ghcup /usr/share/swift || true
          sudo docker image prune -af || true
          echo "After:"; df -h /

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      # Restore the BDD venv itself so setup_environment's reinstall is
      # "already satisfied" (no re-download/rebuild of cryptography + the
      # bdd-executor deps). setup_environment still reinstalls on top, so a
      # requirements/Makefile change is always picked up.
      - name: Cache BDD Python venv
        uses: actions/cache@v4
        with:
          path: tests/bdd/.venv
          key: bdd-venv-py312-${{ hashFiles('testWallet/requirements.txt', 'tests/bdd/Makefile') }}
          restore-keys: |
            bdd-venv-py312-

      # The DSS demo image is the single largest external pull (~1-2GB). Cache a
      # docker-save tarball and load it so kind_load_dss_image's `docker image
      # inspect` skips the pull on a hit.
      - name: Cache DSS demo image
        id: dss-cache
        uses: actions/cache@v4
        with:
          path: /tmp/dss-image.tar
          key: dss-image-conectx-dss-demo-6.2.1
      - name: Load cached DSS image
        if: steps.dss-cache.outputs.cache-hit == 'true'
        run: docker load -i /tmp/dss-image.tar

      # buildx + the GitHub Actions layer cache persist the DCS/pdf-core image
      # layers (base image, go mod download) across runs. The runtime helper
      # exposes the cache tokens `type=gha` needs to a plain `run:` step.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Expose GitHub Actions cache to buildx
        uses: crazy-max/ghaction-github-runtime@v3

      - name: Install kind
        uses: helm/kind-action@v1.10.0
        with:
          version: v0.23.0

      - name: Run kind-backed BDD suite
        run: |
          set -euo pipefail
          make -C tests/bdd run_bdd_kind_ci \
            KIND_CLUSTER_NAME=dcs-bdd \
            DOCKER_IMAGE_REPOSITORY=digital-contracting-service \
            DOCKER_IMAGE_TAG=bdd \
            DOCKER_BUILD="docker buildx build --load" \
            DCS_BUILD_CACHE="--cache-from type=gha,scope=dcs-image-v2 --cache-to type=gha,mode=max,scope=dcs-image-v2" \
            PDF_CORE_BUILD_CACHE="--cache-from type=gha,scope=pdf-core-image-v2 --cache-to type=gha,mode=max,scope=pdf-core-image-v2"

      - name: Save DSS image to cache
        if: always() && steps.dss-cache.outputs.cache-hit != 'true'
        run: docker save conectx/dss-demo:6.2.1 -o /tmp/dss-image.tar || true

      - name: Dump namespace info on failure
        if: failure()
        # kubectl must target the kind cluster's kubeconfig (the Makefile writes
        # it to tests/bdd/.tmp/kind-kubeconfig); otherwise it hits the default
        # context and reports "namespaces dcs-bdd not found", hiding the logs
        # (e.g. a DSS-down flake in the signing suite).
        env:
          KUBECONFIG: ${{ github.workspace }}/tests/bdd/.tmp/kind-kubeconfig
        run: |
          set -euo pipefail
          kubectl get pods -n dcs-bdd -o wide || true
          kubectl get events -n dcs-bdd --sort-by=.lastTimestamp || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=backend \
            -c digital-contracting-service --tail=2000 --prefix || true
          # The backend is not the only container in that pod. A sidecar that
          # never becomes ready holds the whole rollout open, and naming one
          # container here meant the one that failed was the one never logged.
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=backend \
            --all-containers --tail=200 --prefix || true
          # "1/2 Running" does not say which container is unready.
          kubectl get pods -n dcs-bdd -o 'custom-columns=POD:.metadata.name,CONTAINER:.status.containerStatuses[*].name,READY:.status.containerStatuses[*].ready,RESTARTS:.status.containerStatuses[*].restartCount' || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=backend \
            -c digital-contracting-service --tail=200 --prefix --previous || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=pdf-core --tail=100 --prefix || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=dss --tail=100 --prefix || true
          kubectl logs deployment/dcs-keycloak -n dcs-bdd --tail=200 || true
          kubectl logs -n dcs-bdd job/dcs-digital-contracting-service-hsm-provision --tail=100 || true
          kubectl logs -n dcs-bdd job/dcs-digital-contracting-service-fc-realm-provision --tail=100 || true
          # Grepped, not tailed: fc-service logs thousands of DEBUG lines
          # over a full BDD run, so a plain --tail=N from the end reliably
          # misses an early failure. WARN/ERROR (permission denials, auth
          # failures) survive the whole run's volume this way instead.
          kubectl logs -n dcs-bdd deployment/fc-service --tail=-1 | grep -E 'WARN|ERROR|extracted roles|Completed [45]' || true
          # The IPFS node sits under every store, ship and audit anchor, so a
          # node in trouble surfaces as timeouts three layers up while its own
          # reason -- disk pressure, repo limit, a wedged GC -- goes uncaptured.
          # The pin count and repo size are what distinguish a node that
          # degrades as the run grows from one that was never healthy.
          kubectl logs -n dcs-bdd deployment/dcs-ipfs --tail=400 --prefix || true
          docker exec dcs-bdd-control-plane df -h / /var || true
          for release in dcs dcs2; do
            echo "--- ${release}-ipfs node state ---"
            kubectl exec -n dcs-bdd "deployment/${release}-ipfs" -c ipfs -- \
              sh -c 'ipfs pin ls --type=recursive 2>/dev/null | wc -l' || true
            kubectl exec -n dcs-bdd "deployment/${release}-ipfs" -c ipfs -- \
              ipfs repo stat || true
          done

      - name: Generate coverage XML and summary
        if: always()
        run: |
          set -euo pipefail
          if [[ -f .coverage ]] && [[ -x tests/bdd/.venv/bin/coverage ]]; then
            tests/bdd/.venv/bin/coverage xml -o tests/bdd/.reports/coverage.xml
            tests/bdd/.venv/bin/coverage report | tee tests/bdd/.reports/coverage.txt
            {
              echo "## BDD Coverage"
              echo ""
              echo '```text'
              cat tests/bdd/.reports/coverage.txt
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No coverage data file found (.coverage) or coverage binary missing; skipping coverage summary."
          fi

      - name: Publish BDD test report
        if: always() && hashFiles('tests/bdd/.reports/junit/*.xml') != ''
        uses: dorny/test-reporter@v1
        with:
          name: BDD Test Results
          path: tests/bdd/.reports/junit/*.xml
          reporter: java-junit
          fail-on-error: false
          list-suites: all
          list-tests: all

      - name: Upload BDD reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bdd-reports
          path: tests/bdd/.reports/**
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30

      - name: Teardown kind cluster
        if: always()
        run: |
          set -euo pipefail
          make -C tests/bdd kind_delete KIND_CLUSTER_NAME=dcs-bdd || true

  audit-isolated-ci-gate:
    name: audit-isolated-ci-gate
    permissions:
      contents: read
      checks: write
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: audit-isolated-ci-gate-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Before:"; df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
            /opt/hostedtoolcache/CodeQL /usr/local/.ghcup /usr/share/swift || true
          sudo docker image prune -af || true
          echo "After:"; df -h /

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Cache audit BDD Python venv
        uses: actions/cache@v4
        with:
          path: tests/bdd/.venv
          key: audit-bdd-venv-py312-${{ hashFiles('testWallet/requirements.txt', 'tests/bdd/Makefile') }}
          restore-keys: |
            audit-bdd-venv-py312-

      - name: Cache DSS demo image
        id: audit-dss-cache
        uses: actions/cache@v4
        with:
          path: /tmp/audit-dss-image.tar
          key: audit-dss-image-conectx-dss-demo-6.2.1
      - name: Load cached DSS image
        if: steps.audit-dss-cache.outputs.cache-hit == 'true'
        run: docker load -i /tmp/audit-dss-image.tar

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Expose GitHub Actions cache to buildx
        uses: crazy-max/ghaction-github-runtime@v3

      - name: Install kind
        uses: helm/kind-action@v1.10.0
        with:
          version: v0.23.0

      - name: Run isolated audit BDD gate
        run: |
          set -euo pipefail
          make -C tests/bdd run_bdd_audit_kind_ci \
            KIND_CLUSTER_NAME=dcs-audit-bdd \
            DOCKER_IMAGE_REPOSITORY=digital-contracting-service \
            DOCKER_IMAGE_TAG=bdd \
            DOCKER_BUILD="docker buildx build --load" \
            DCS_BUILD_CACHE="--cache-from type=gha,scope=audit-dcs-image --cache-to type=gha,mode=max,scope=audit-dcs-image" \
            PDF_CORE_BUILD_CACHE="--cache-from type=gha,scope=audit-pdf-core-image --cache-to type=gha,mode=max,scope=audit-pdf-core-image"

      - name: Save DSS image to cache
        if: always() && steps.audit-dss-cache.outputs.cache-hit != 'true'
        run: docker save conectx/dss-demo:6.2.1 -o /tmp/audit-dss-image.tar || true

      - name: Dump audit namespace info on failure
        if: failure()
        env:
          KUBECONFIG: ${{ github.workspace }}/tests/bdd/.tmp/kind-kubeconfig
        run: |
          set -euo pipefail
          kubectl get pods -n dcs-bdd -o wide || true
          kubectl get events -n dcs-bdd --sort-by=.lastTimestamp || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=backend \
            -c digital-contracting-service --tail=2000 --prefix || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=backend \
            --all-containers --tail=200 --prefix || true
          kubectl get pods -n dcs-bdd -o 'custom-columns=POD:.metadata.name,CONTAINER:.status.containerStatuses[*].name,READY:.status.containerStatuses[*].ready,RESTARTS:.status.containerStatuses[*].restartCount' || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=pdf-core --tail=200 --prefix || true
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=dss --tail=100 --prefix || true
          kubectl logs deployment/dcs-keycloak -n dcs-bdd --tail=200 || true
          kubectl logs -n dcs-bdd job/dcs-digital-contracting-service-hsm-provision --tail=100 || true
          kubectl logs -n dcs-bdd job/dcs-digital-contracting-service-fc-realm-provision --tail=100 || true
          kubectl logs -n dcs-bdd deployment/fc-service --tail=-1 | grep -E 'WARN|ERROR|extracted roles|Completed [45]' || true

      - name: Publish audit BDD test report
        if: always() && hashFiles('tests/bdd/.reports/junit/*.xml') != ''
        uses: dorny/test-reporter@v1
        with:
          name: Audit Isolated BDD Test Results
          path: tests/bdd/.reports/junit/*.xml
          reporter: java-junit
          fail-on-error: false
          list-suites: all
          list-tests: all

      - name: Upload audit BDD reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audit-isolated-bdd-reports
          path: tests/bdd/.reports/**
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30

      - name: Teardown audit kind cluster
        if: always()
        run: |
          set -euo pipefail
          make -C tests/bdd kind_delete KIND_CLUSTER_NAME=dcs-audit-bdd || true

  e2e-playwright:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    # Sized so a bad day still finishes: the suite retries each failure once
    # and a killed run writes no HTML report, which is exactly when the
    # artifact is needed.
    timeout-minutes: 80
    concurrency:
      group: e2e-playwright-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free up disk space
        run: |
          echo "Before:"; df -h /
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android \
            /opt/hostedtoolcache/CodeQL /usr/local/.ghcup /usr/share/swift || true
          sudo docker image prune -af || true
          echo "After:"; df -h /

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      # Restore the BDD venv itself so setup_environment's reinstall is
      # "already satisfied" (no re-download/rebuild of cryptography + the
      # bdd-executor deps). setup_environment still reinstalls on top, so a
      # requirements/Makefile change is always picked up.
      - name: Cache BDD Python venv
        uses: actions/cache@v4
        with:
          path: tests/bdd/.venv
          key: bdd-venv-py312-${{ hashFiles('testWallet/requirements.txt', 'tests/bdd/Makefile') }}
          restore-keys: |
            bdd-venv-py312-

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/ClientApp/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend/ClientApp

      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('frontend/ClientApp/package-lock.json') }}

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: frontend/ClientApp

      - name: Cache DSS demo image
        id: dss-cache
        uses: actions/cache@v4
        with:
          path: /tmp/dss-image.tar
          key: dss-image-conectx-dss-demo-6.2.1
      - name: Load cached DSS image
        if: steps.dss-cache.outputs.cache-hit == 'true'
        run: docker load -i /tmp/dss-image.tar

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Expose GitHub Actions cache to buildx
        uses: crazy-max/ghaction-github-runtime@v3

      - name: Install kind
        uses: helm/kind-action@v1.10.0
        with:
          version: v0.23.0

      - name: Run Playwright E2E on its own kind stack
        run: |
          set -euo pipefail
          make -C tests/bdd run_e2e_kind_ci \
            KIND_CLUSTER_NAME=dcs-e2e \
            DOCKER_IMAGE_REPOSITORY=digital-contracting-service \
            DOCKER_IMAGE_TAG=bdd \
            DOCKER_BUILD="docker buildx build --load" \
            DCS_BUILD_CACHE="--cache-from type=gha,scope=dcs-image-v2 --cache-to type=gha,mode=max,scope=dcs-image-v2" \
            PDF_CORE_BUILD_CACHE="--cache-from type=gha,scope=pdf-core-image-v2 --cache-to type=gha,mode=max,scope=pdf-core-image-v2"

      - name: Save DSS image to cache
        if: always() && steps.dss-cache.outputs.cache-hit != 'true'
        run: docker save conectx/dss-demo:6.2.1 -o /tmp/dss-image.tar || true

      - name: Upload Playwright report
        if: always() && hashFiles('frontend/ClientApp/playwright-report/**') != ''
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/ClientApp/playwright-report/**
          retention-days: 30

      - name: Upload vertical PDF artifacts
        if: always() && hashFiles('frontend/ClientApp/vertical-artifacts/**') != ''
        uses: actions/upload-artifact@v4
        with:
          name: vertical-pdf-artifacts
          path: frontend/ClientApp/vertical-artifacts/**
          retention-days: 30

      - name: Dump namespace info on failure
        if: failure()
        # The e2e stack runs in the dcs-e2e kind cluster, whose kubeconfig the
        # Makefile writes to tests/bdd/.tmp/kind-kubeconfig. Without pointing
        # kubectl at it this step used the default context and reported
        # "namespaces dcs-bdd not found", hiding both instances' logs. The
        # backend selector matches BOTH releases (dcs + dcs2), so --prefix
        # distinguishes instance A's synchronizer/pdf-gen from instance B's PostPdf.
        env:
          KUBECONFIG: ${{ github.workspace }}/tests/bdd/.tmp/kind-kubeconfig
        run: |
          set -euo pipefail
          kubectl get pods -n dcs-bdd -o wide || true
          kubectl get events -n dcs-bdd --sort-by=.lastTimestamp || true
          # Per-deployment so both instances (dcs + dcs2) are captured whole and a
          # large tail keeps the export/regeneration diagnostics that fire near the
          # end of a run inside the window.
          for dep in $(kubectl get deploy -n dcs-bdd -o name | grep -E 'digital-contracting-service$'); do
            echo "===== logs $dep ====="
            kubectl logs -n dcs-bdd "$dep" -c digital-contracting-service --tail=2000 --prefix || true
          done
          kubectl logs -n dcs-bdd -l app.kubernetes.io/component=pdf-core --tail=400 --prefix || true
          kubectl logs deployment/dcs-keycloak -n dcs-bdd --tail=100 || true
          kubectl logs -n dcs-bdd job/dcs-digital-contracting-service-fc-realm-provision --tail=100 || true
          kubectl logs -n dcs-bdd deployment/fc-service --tail=-1 | grep -E 'WARN|ERROR|extracted roles|Completed [45]' || true

      - name: Teardown kind cluster
        if: always()
        run: |
          set -euo pipefail
          make -C tests/bdd kind_delete KIND_CLUSTER_NAME=dcs-e2e || true
