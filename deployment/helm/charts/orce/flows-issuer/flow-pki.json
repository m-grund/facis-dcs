[
  {
    "id": "pki-tab",
    "type": "tab",
    "label": "PKI Setup",
    "disabled": false,
    "info": ""
  },
  {
    "id": "pki-init",
    "type": "inject",
    "z": "pki-tab",
    "name": "on deploy",
    "props": [
      {
        "p": "payload"
      }
    ],
    "repeat": "",
    "once": true,
    "onceDelay": 0.1,
    "topic": "",
    "payload": "",
    "payloadType": "date",
    "x": 150,
    "y": 100,
    "wires": [
      [
        "pki-ensure"
      ]
    ]
  },
  {
    "id": "pki-ensure",
    "type": "function",
    "z": "pki-tab",
    "name": "ensure root CA + issuer cert",
    "func": "const pkiDir = '/data/pki';\nconst opensslBin = '/usr/bin/openssl';\n\nconst rootKeyPath = pkiDir + '/root-ca.key';\nconst rootCrtPath = pkiDir + '/root-ca.crt';\nconst issuerKeyPath = pkiDir + '/issuer.key';\nconst issuerCsrPath = pkiDir + '/issuer.csr';\nconst issuerCrtPath = pkiDir + '/issuer.crt';\nconst issuerExtPath = pkiDir + '/issuer-ext.cnf';\n// Mounted only where a deployment hands this issuer its root CA instead of\n// letting it generate one (orce.pkiRootCA.devFixture).\nconst fixtureRootCrt = '/etc/orce/pki-root/root-ca.crt';\nconst fixtureRootKey = '/etc/orce/pki-root/root-ca.key';\n// The issuer's OWN key, mounted for the same reason as the root: a login\n// issuer is verified against the key an operator wrote down (ADR-35), and a\n// key generated on this volume at boot is not knowable when that trust\n// document is written. Where it is mounted the committed one is adopted, so\n// the stack's configuration can pin it in advance.\nconst fixtureIssuerKey = '/etc/orce/pki-root/issuer.key';\n\nfs.mkdirSync(pkiDir, { recursive: true });\n\nfunction sh(cmd) {\n    return cp.execSync(cmd, { cwd: pkiDir, stdio: ['ignore', 'pipe', 'pipe'] });\n}\n\n// This container's clock can be far off real-world time (sandboxed demo\n// environments sometimes run with a future system date). If notBefore is\n// stamped at container \"now\" and that's in the future relative to a real\n// device (e.g. the wallet holder's phone), every verifier rejects the cert\n// as \"not yet valid\". openssl's req/x509 apps have no -not_before flag, so\n// backdate generation with faketime instead: notBefore lands in 2020\n// regardless of container time, with -days sized to still reach ~2040.\nfunction faketime() {\n    try {\n        cp.execSync('which faketime', { stdio: 'ignore' });\n    } catch (e) {\n        sh('apk add --no-cache libfaketime');\n    }\n    return 'faketime \"2020-01-01 00:00:00\" ';\n}\n\n// did:web with path segments: authority first, then each path segment,\n// colon-separated (w3c-ccg did-method-web). The credential's `iss`, the\n// did.json this issuer serves and the certificate's SAN all have to agree,\n// so they all derive the identifier here.\nfunction issuerDidFor(baseUrl) {\n    const rest = baseUrl.replace(/^[a-z]+:\\/\\//i, '');\n    const authority = rest.split('/')[0];\n    const segs = rest.split('/').slice(1).filter(Boolean);\n    return 'did:web:' + encodeURIComponent(authority) + (segs.length ? ':' + segs.join(':') : '');\n}\n\nfunction publishPki() {\n    const rootCaCertPem = fs.readFileSync(rootCrtPath, 'utf8');\n    const issuerCertPem = fs.readFileSync(issuerCrtPath, 'utf8');\n    const issuerKeyPem = fs.readFileSync(issuerKeyPath, 'utf8');\n    const pki = {\n        issuerKeyPem,\n        issuerCertPem,\n        issuerCertDerB64: new crypto.X509Certificate(issuerCertPem).raw.toString('base64'),\n        rootCaCertPem,\n        rootCaCertDerB64: new crypto.X509Certificate(rootCaCertPem).raw.toString('base64')\n    };\n    global.set('pki', pki);\n    return pki;\n}\n\n// A verifier that resolves this issuer's key from a presented chain checks\n// that the leaf NAMES the issuer the token claims to come from -- a chain\n// only proves the root vouched for the certificate, not whose it is. DCS\n// matches a SAN URI against `iss`, a SAN DNS name against the identifier's\n// authority, or the subject CN exactly\n// (backend/internal/auth/oid4vp/sdjwt/keys.go, leafIdentifiesIssuer), and\n// applies the same check to a status list as to a credential (ADR-34).\n//\n// The public URL is not knowable when this pod boots: it depends on the\n// ingress in front of it, including a stripped path prefix that only reaches\n// Node-RED as a request header. So the leaf is minted on the first request\n// that arrives through the real URL, and again whenever that URL changes.\n// Nothing ever mints a SAN-less leaf: one could not be verified by anything,\n// and serving it is what made a correctly-signed status list unusable.\n//\n// The three SANs cover the three identifiers this issuer answers to. The DID\n// is the one that always works: a did:web authority carrying a port arrives\n// percent-encoded and decodes to host:port, which no DNS name can equal. The\n// https base URL is what a status list's `iss` is, and the hostname is for\n// wallets that match on DNS alone.\n//\n// The issuer KEY is kept across reissues, so a new leaf invalidates neither\n// credentials already issued nor the key published in did.json and /pki/jwks.json.\nfunction ensureIssuerCertFor(baseUrl, force) {\n    const did = issuerDidFor(baseUrl);\n    const hostname = baseUrl.replace(/^[a-z]+:\\/\\//i, '').split(/[/:]/)[0];\n\n    if (!force && fs.existsSync(issuerCrtPath)) {\n        const current = new crypto.X509Certificate(fs.readFileSync(issuerCrtPath, 'utf8'));\n        // Whole SAN entries, never a substring of the joined list: one DID is a\n        // prefix of another whenever a path segment is added, so\n        // did:web:host%3A30181 is found inside did:web:host%3A30181:issuer. An\n        // issuer that moved out from behind a path prefix would keep the leaf\n        // naming the prefixed identity and sign every list with a certificate\n        // that names someone else, which is refused as an unidentified issuer.\n        const sans = String(current.subjectAltName || '').split(',').map(function (entry) {\n            return entry.trim();\n        });\n        if (sans.indexOf('URI:' + did) !== -1) {\n            const pki = global.get('pki');\n            return (pki && pki.issuerKeyPem) ? pki : publishPki();\n        }\n    }\n\n    fs.writeFileSync(\n        issuerExtPath,\n        '[v3_issuer]\\nkeyUsage=critical,digitalSignature\\n' +\n        'subjectAltName=URI:' + did + ',URI:' + baseUrl + ',DNS:' + hostname + '\\n'\n    );\n    if (!fs.existsSync(issuerKeyPath)) {\n        sh(`${opensslBin} ecparam -name prime256v1 -genkey -noout -out \"${issuerKeyPath}\"`);\n    }\n    sh(\n        `${opensslBin} req -new -key \"${issuerKeyPath}\" -out \"${issuerCsrPath}\" ` +\n        `-subj \"/CN=FACIS Demo Issuer\"`\n    );\n    sh(\n        faketime() + `${opensslBin} x509 -req -in \"${issuerCsrPath}\" ` +\n        `-CA \"${rootCrtPath}\" -CAkey \"${rootKeyPath}\" -CAcreateserial ` +\n        `-out \"${issuerCrtPath}\" -days 7300 ` +\n        `-extfile \"${issuerExtPath}\" -extensions v3_issuer`\n    );\n\n    const pki = publishPki();\n    node.log('pki: issued leaf for ' + did + ' (SAN: ' + new crypto.X509Certificate(pki.issuerCertPem).subjectAltName + ')');\n    return pki;\n}\n\nglobal.set('issuerDidFor', issuerDidFor);\nglobal.set('ensureIssuerCertFor', ensureIssuerCertFor);\n\nconst haveRootCa = fs.existsSync(rootKeyPath) && fs.existsSync(rootCrtPath);\n\nif (!haveRootCa && fs.existsSync(fixtureRootCrt)) {\n    // A verifier can only anchor a root it can name, and a root generated here\n    // does not exist until this pod has booted -- while every generated root\n    // carries the same subject, so it cannot be told from another by name\n    // either. A stack whose verifier configuration is committed in advance\n    // therefore hands the root in and the issuer signs under it.\n    fs.copyFileSync(fixtureRootCrt, rootCrtPath);\n    fs.copyFileSync(fixtureRootKey, rootKeyPath);\n    node.log('pki: adopted the mounted root CA');\n} else if (!haveRootCa) {\n    sh(`${opensslBin} ecparam -name prime256v1 -genkey -noout -out \"${rootKeyPath}\"`);\n    sh(\n        faketime() + `${opensslBin} req -x509 -new -key \"${rootKeyPath}\" -out \"${rootCrtPath}\" ` +\n        `-days 7300 -subj \"/CN=FACIS Demo Root CA\" ` +\n        `-addext \"basicConstraints=critical,CA:true\"`\n    );\n    node.log('pki: generated root CA');\n} else {\n    node.log('pki: root CA already present, skipping');\n}\n\nif (!fs.existsSync(issuerKeyPath) && fs.existsSync(fixtureIssuerKey)) {\n    fs.copyFileSync(fixtureIssuerKey, issuerKeyPath);\n    node.log('pki: adopted the mounted issuer key (the leaf follows on the first request, once the public URL is known)');\n} else if (!fs.existsSync(issuerKeyPath)) {\n    sh(`${opensslBin} ecparam -name prime256v1 -genkey -noout -out \"${issuerKeyPath}\"`);\n    node.log('pki: generated issuer key (the leaf follows on the first request, once the public URL is known)');\n}\n\nnode.status({ fill: 'green', shape: 'dot', text: 'pki ready' });\nreturn null;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "cp",
        "module": "child_process"
      },
      {
        "var": "fs",
        "module": "fs"
      },
      {
        "var": "crypto",
        "module": "crypto"
      }
    ],
    "x": 420,
    "y": 100,
    "wires": [
      []
    ]
  },
  {
    "id": "pki-get-rootca",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /pki/root-ca.pem",
    "url": "/pki/root-ca.pem",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 180,
    "y": 220,
    "wires": [
      [
        "pki-serve-rootca"
      ]
    ]
  },
  {
    "id": "pki-serve-rootca",
    "type": "function",
    "z": "pki-tab",
    "name": "serve root CA",
    "func": "// The leaf is minted for the URL a request arrives through (see the \"ensure\n// root CA + issuer cert\" node): a certificate that names no issuer verifies\n// nowhere, so nothing here reads one off disk behind the helper's back.\nconst pki = global.get('ensureIssuerCertFor')(global.get('getBaseUrl')(msg));\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/x-pem-file' };\nmsg.payload = pki.rootCaCertPem;\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 430,
    "y": 220,
    "wires": [
      [
        "pki-resp-rootca"
      ]
    ]
  },
  {
    "id": "pki-resp-rootca",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 660,
    "y": 220,
    "wires": []
  },
  {
    "id": "pki-get-issuer",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /pki/issuer.pem",
    "url": "/pki/issuer.pem",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 180,
    "y": 300,
    "wires": [
      [
        "pki-serve-issuer"
      ]
    ]
  },
  {
    "id": "pki-serve-issuer",
    "type": "function",
    "z": "pki-tab",
    "name": "serve issuer cert",
    "func": "// The leaf is minted for the URL a request arrives through (see the \"ensure\n// root CA + issuer cert\" node): a certificate that names no issuer verifies\n// nowhere, so nothing here reads one off disk behind the helper's back.\nconst pki = global.get('ensureIssuerCertFor')(global.get('getBaseUrl')(msg));\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/x-pem-file' };\nmsg.payload = pki.issuerCertPem;\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 430,
    "y": 300,
    "wires": [
      [
        "pki-resp-issuer"
      ]
    ]
  },
  {
    "id": "pki-resp-issuer",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 660,
    "y": 300,
    "wires": []
  },
  {
    "id": "pki-get-chain",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /pki/chain.pem",
    "url": "/pki/chain.pem",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 180,
    "y": 380,
    "wires": [
      [
        "pki-serve-chain"
      ]
    ]
  },
  {
    "id": "pki-serve-chain",
    "type": "function",
    "z": "pki-tab",
    "name": "serve chain (issuer + root)",
    "func": "// The leaf is minted for the URL a request arrives through (see the \"ensure\n// root CA + issuer cert\" node): a certificate that names no issuer verifies\n// nowhere, so nothing here reads one off disk behind the helper's back.\nconst pki = global.get('ensureIssuerCertFor')(global.get('getBaseUrl')(msg));\nconst issuerPem = pki.issuerCertPem.endsWith('\\n') ? pki.issuerCertPem : pki.issuerCertPem + '\\n';\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/x-pem-file' };\nmsg.payload = issuerPem + pki.rootCaCertPem;\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 440,
    "y": 380,
    "wires": [
      [
        "pki-resp-chain"
      ]
    ]
  },
  {
    "id": "pki-resp-chain",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 660,
    "y": 380,
    "wires": []
  },
  {
    "id": "pki-get-reissue",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /pki/reissue",
    "url": "/pki/reissue",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 190,
    "y": 460,
    "wires": [
      [
        "pki-do-reissue"
      ]
    ]
  },
  {
    "id": "pki-do-reissue",
    "type": "function",
    "z": "pki-tab",
    "name": "reissue issuer cert with SAN=baseUrl",
    "func": "// Force a fresh issuer leaf for whatever URL this request arrived through.\n// The certificate names the issuer it signs for through its SANs (see the\n// \"ensure root CA + issuer cert\" node, ensureIssuerCertFor, for which SANs\n// and why). Every request path that serves or signs with the leaf calls the\n// same helper, so this endpoint is only the manual override for re-minting\n// against an unchanged URL -- hit it through the URL clients actually use.\n// The root CA is untouched, so /pki/root-ca.pem stays a valid trust anchor.\nconst baseUrl = global.get('getBaseUrl')(msg);\nconst hostname = baseUrl.replace(/^[a-z]+:\\/\\//i, '').split(/[/:]/)[0];\n\nconst pki = global.get('ensureIssuerCertFor')(baseUrl, true);\nconst cert = new crypto.X509Certificate(pki.issuerCertPem);\n\nnode.status({ fill: 'green', shape: 'dot', text: 'reissued for ' + hostname });\n\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/json' };\nmsg.payload = {\n    reissued: true,\n    baseUrl,\n    hostname,\n    issuerDid: global.get('issuerDidFor')(baseUrl),\n    subjectAltName: cert.subjectAltName\n};\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "cp",
        "module": "child_process"
      },
      {
        "var": "fs",
        "module": "fs"
      },
      {
        "var": "crypto",
        "module": "crypto"
      }
    ],
    "x": 470,
    "y": 460,
    "wires": [
      [
        "pki-resp-reissue"
      ]
    ]
  },
  {
    "id": "pki-resp-reissue",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 700,
    "y": 460,
    "wires": []
  },
  {
    "id": "pki-get-jwks",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /pki/jwks.json",
    "url": "/pki/jwks.json",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 180,
    "y": 540,
    "wires": [
      [
        "pki-serve-jwks"
      ]
    ]
  },
  {
    "id": "pki-serve-jwks",
    "type": "function",
    "z": "pki-tab",
    "name": "serve issuer JWKS",
    "func": "// The credential-issuance tab embeds this same key as a `jwk` header on\n// every issued credential, and the did.json tab publishes it as a\n// verification method, so a verifier configured with mechanism jwks or\n// did:web reaches the same key either way. Publishing it here lets anyone\n// (an operator writing a trust entry, an auditor) pull it out-of-band too.\n// The leaf is minted for the URL a request arrives through (see the \"ensure\n// root CA + issuer cert\" node): a certificate that names no issuer verifies\n// nowhere, so nothing here reads one off disk behind the helper's back.\nconst pki = global.get('ensureIssuerCertFor')(global.get('getBaseUrl')(msg));\nconst publicKey = crypto.createPublicKey(crypto.createPrivateKey(pki.issuerKeyPem));\nconst jwk = publicKey.export({ format: 'jwk' });\nconst cleanJwk = { kty: jwk.kty, crv: jwk.crv, x: jwk.x, y: jwk.y };\nconst kid = crypto.createHash('sha256').update(JSON.stringify(cleanJwk)).digest('base64url');\n\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/json' };\nmsg.payload = { keys: [{ ...cleanJwk, kid, use: 'sig', alg: 'ES256' }] };\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "crypto",
        "module": "crypto"
      }
    ],
    "x": 440,
    "y": 540,
    "wires": [
      [
        "pki-resp-jwks"
      ]
    ]
  },
  {
    "id": "pki-resp-jwks",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 670,
    "y": 540,
    "wires": []
  },
  {
    "id": "pki-get-diddoc",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /.well-known/did.json",
    "url": "/.well-known/did.json",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 200,
    "y": 620,
    "wires": [
      [
        "pki-serve-diddoc"
      ]
    ]
  },
  {
    "id": "pki-serve-diddoc",
    "type": "function",
    "z": "pki-tab",
    "name": "serve did:web document",
    "func": "// Paradym's SD-JWT VC verifier only supports resolving the issuer's\n// signing key via a DID or an x5c chain (\"unsupported signing method\" is\n// its exact error otherwise) -- it does not accept a bare embedded `jwk`\n// header. DCS resolves an issuer's key by the mechanism its trust entry\n// declares (ADR-31): a did:web entry is resolved live from this document,\n// a jwks entry from keys bundled in the trust file. did:web is therefore\n// the one identity both sides can be pointed at. See the\n// credential-issuance function for where `iss` and the proof-header `kid`\n// are set to this DID.\nconst baseUrl = global.get('getBaseUrl')(msg);\n// Same derivation as the certificate's SAN URI, from the same helper, so the\n// document and the leaf cannot disagree about who this issuer is.\nconst did = global.get('issuerDidFor')(baseUrl);\nconst pki = global.get('ensureIssuerCertFor')(baseUrl);\n\nconst publicKey = crypto.createPublicKey(crypto.createPrivateKey(pki.issuerKeyPem));\nconst jwk = publicKey.export({ format: 'jwk' });\nconst cleanJwk = { kty: jwk.kty, crv: jwk.crv, x: jwk.x, y: jwk.y, alg: 'ES256', use: 'sig' };\nconst keyId = did + '#key-1';\n\nmsg.statusCode = 200;\nmsg.headers = { 'content-type': 'application/did+ld+json' };\nmsg.payload = {\n    '@context': [\n        'https://www.w3.org/ns/did/v1',\n        'https://w3id.org/security/suites/jws-2020/v1'\n    ],\n    id: did,\n    verificationMethod: [{\n        id: keyId,\n        type: 'JsonWebKey2020',\n        controller: did,\n        publicKeyJwk: cleanJwk\n    }],\n    assertionMethod: [keyId],\n    authentication: [keyId]\n};\nreturn msg;",
    "outputs": 1,
    "timeout": 0,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [
      {
        "var": "crypto",
        "module": "crypto"
      }
    ],
    "x": 460,
    "y": 620,
    "wires": [
      [
        "pki-resp-diddoc"
      ]
    ]
  },
  {
    "id": "pki-resp-diddoc",
    "type": "http response",
    "z": "pki-tab",
    "name": "",
    "statusCode": "",
    "headers": {},
    "x": 690,
    "y": 620,
    "wires": []
  },
  {
    "id": "pki-get-diddoc-pathbased",
    "type": "http in",
    "z": "pki-tab",
    "name": "GET /did.json (path-based did:web)",
    "url": "/did.json",
    "method": "get",
    "upload": false,
    "swaggerDoc": "",
    "x": 200,
    "y": 660,
    "wires": [
      [
        "pki-serve-diddoc"
      ]
    ]
  }
]
